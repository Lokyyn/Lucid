name: .NET 6 Build

on:
  push:
    branches: [trunk ]
  pull_request:
    branches: [trunk ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET 🛠
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies 🔁
      run: dotnet restore source/LokConLib.sln
    - name: Build 🚧
      run: dotnet build source/LokConLib.sln --no-restore 
    - name: Test 🧪
      run: dotnet test source/LokConLib.sln --no-build --verbosity normal
